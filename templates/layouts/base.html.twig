<!DOCTYPE html>
<html lang="{{app.request.locale}}">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scal=1, shrink-to-fit=no">
		<title>
			{% block title 'Welcome!' %}
		</title>
		{# Run `composer require symfony/webpack-encore-bundle` #}
		{# and uncomment the following Encore helpers to start using Symfony UX #}
		{% block stylesheets %}
		{% include "/layouts/style.html.twig" %}
			{#{{ encore_entry_link_tags('app') }}#}
		{% endblock %}
	</head>
	<body>
	{{include('layouts/partials/_navbar.html.twig')}}
	<div class="flex flex-col justify-between items-center">
	{{include('layouts/partials/_flash_msg.html.twig')}}
		{# <nav>
			<ul>
				<li>
					<a href="{{ path('app_home')}}">Go Home</a>
				</li>
				<li>
					<a href="{{ path('pin_create')}}">Create new pin</a>
				</li>
			</ul>
		</nav> #}
		<main role="main" class="container ">
		<div class="row">
		<div class="col-md-4 mx-auto">
		{% block body %}{% endblock %}
		</div></div>
		</main>
		{{ include('layouts/partials/_footer.html.twig') }}
		{% block javascripts %}
			{% include "/layouts/js.html.twig" %}
		{% endblock %}
		</div>
	</body>
</html>
